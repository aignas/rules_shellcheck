---
bcr_test_module:
  module_path: "examples/check_glob"
  matrix:
    platform:
      - "debian10"
      - "macos"
      - "macos_arm64"
      - "ubuntu2004"
      - "ubuntu2004_arm64"
    bazel: [7.*, 8.*, 9.*]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
