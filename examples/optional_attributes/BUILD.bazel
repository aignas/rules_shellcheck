load("@com_github_aignas_rules_shellcheck//:def.bzl", "shellcheck_test")

shellcheck_test(
    name = "shellcheck_all",
    data = glob(["*.sh"]),
    format = "gcc",
    severity = "warning",
    tags = ["lint"],
)
